Solid direction. Here’s a clean next-step checklist:

**✅ Done:**

* Callum-style static mods on Nav + Num layers
* HRM order optimized for pinky load
* Modern QMK behavior tuning (FlowTap, ChordalHold, RetroTap)

**🔧 To-Do (Next Iteration):**

1. Replace single-tap Mouse layer key → **Leader**; double-tap/hold → **Mouse**.
2. Add **Leader-based app launching** (workspace actions, scripts).
3. Retrain muscle memory with new HRM order (short sessions).
4. Test **Graphite** vs **Hands Down Neu** for main alpha layout.
5. Evaluate **HRM removal** if Callum layer covers needs.
6. Create **Win-Move Nav Layer** (Mouse+Nav combo = Win hold).
7. Expand **Function Layer**: `Ctrl+C/V/X/A` shortcuts + system actions.

Optional: test **One-Shot Win** for Hyprland meta actions to cut pinky load further.

